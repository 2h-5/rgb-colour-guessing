# RGB Colour Game (Deployment)

## Features (Compared to Other `Branches`)

###### This `branch` is used for the web deployment of this project, so that if I am tweaking for the "README.md" in the `main` branch, the commits are not going to influence the deployed page✌️!
